extends layout

block content

  h1=user.name

  =JSON.stringify(connections)

  h2 Connections

  ul
    for connection in connections
      li
        a(href='/users/' + connection.id)=connection.name